import "./scss/Login.scss";

const Login = () => {
  return (
    <main>
      <div className="login">
        <h2>로그인</h2>
        <p>Wavve 계정으로 로그인</p>
      </div>
    </main>
  );
};

export default Login;
